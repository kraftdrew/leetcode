# key = : parameter in functions like min(), max(), and sorted() is used 
# to specify a function that determines the "sort key" or "comparison key" for each element.

_var = min(["flower","flow","fligh"], key = len)